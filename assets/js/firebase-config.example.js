// Template for a Firebase project configuration.
// Not loaded by any page — this is onboarding documentation only, e.g. if
// this site template is reused for a different brand/project and needs to
// point at a different Firebase project. Firebase web config values are not
// secret (security is enforced by Firestore Security Rules), so the real
// file (firebase-config.js, same shape) is committed directly rather than
// injected via environment variables.
export const firebaseConfig = {
  apiKey: "your-api-key",
  authDomain: "your-project-id.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "your-sender-id",
  appId: "your-app-id",
  measurementId: "your-measurement-id"
};
